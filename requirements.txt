# Install the project itself in editable mode so that core/ and app/
# are importable as top-level packages on Streamlit Cloud (each MPA
# page runs in its own script runner that does not inherit main.py's
# sys.path modifications). pyproject.toml declares packages.find
# for include=["core*", "app*"].
-e .

streamlit>=1.35
simpy>=4.1
numpy>=1.26
pandas>=2.2
scipy>=1.13
plotly>=5.22
pydantic>=2.7
loguru>=0.7
openpyxl>=3.1
SALib>=1.5
pytest>=8.2
